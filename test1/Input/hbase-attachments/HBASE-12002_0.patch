diff --git a/src/main/docbkx/book.xml b/src/main/docbkx/book.xml
index 8fc2f7a..b8767d8 100644
--- a/src/main/docbkx/book.xml
+++ b/src/main/docbkx/book.xml
@@ -574,7 +574,8 @@ create 'bar', 'fam'
         <para>The following is an example of a on an HTable table instance. Assume that a table is
           populated with rows with keys "row1", "row2", "row3", and then another set of rows with
           the keys "abc1", "abc2", and "abc3". The following example shows how startRow and stopRow
-          can be applied to a Scan instance to return the rows beginning with "row".</para>
+          can be set to a Scan instance to return the rows beginning with "row" by using the
+          setRowPrefix helper function.</para>
         <programlisting language="java">
 public static final byte[] CF = "cf".getBytes();
 public static final byte[] ATTR = "attr".getBytes();
@@ -584,8 +585,7 @@ HTable htable = ...      // instantiate HTable
 
 Scan scan = new Scan();
 scan.addColumn(CF, ATTR);
-scan.setStartRow(Bytes.toBytes("row")); // start key is inclusive
-scan.setStopRow(Bytes.toBytes("rox"));  // stop key is exclusive
+scan.setRowPrefix(Bytes.toBytes("row"));
 ResultScanner rs = htable.getScanner(scan);
 try {
   for (Result r = rs.next(); r != null; r = rs.next()) {
@@ -594,7 +594,15 @@ try {
   rs.close();  // always close the ResultScanner!
 }
 </programlisting>
-        <para>Note that generally the easiest way to specify a specific stop point for a scan is by
+        <para>In older versions of HBase the setRowPrefix method was not yet available. In those
+          versions of HBase the startRow must be set to the prefix and the stopRow must be set to
+          <emphasis>the first prefix directly following the last wanted row </emphasis>.
+        In the "row" example this can simply be done as follows</para>
+<programlisting language="java">
+scan.setStartRow(Bytes.toBytes("row")); // start key is inclusive
+scan.setStopRow(Bytes.toBytes("rox"));  // stop key is exclusive
+</programlisting>
+        <para>Alternatively it is possible to specify a specific stop point for a scan is by
           using the <link
             xlink:href="http://hbase.apache.org/apidocs/org/apache/hadoop/hbase/filter/InclusiveStopFilter.html">InclusiveStopFilter</link>
           class. </para>
