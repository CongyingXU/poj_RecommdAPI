public class Server {
  public Value getValue(boolean simple) {
    Value value = new Value();
    value.setAny(simple ? "Hello" : new Value());
    return value;
  }

  public Object getAny(boolean simple) {
    return simple ? "Hello" : new Value();
  }
}
